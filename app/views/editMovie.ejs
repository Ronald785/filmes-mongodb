<!DOCTYPE html>
<html lang="pt-br"><head>
<meta http-equiv="content-type" content="text/html; charset=UTF-8">
	<meta charset="utf-8">
	<title>Formulário de Editar Filme</title>
    <link rel="stylesheet" type="text/css" href="/styles/home.css" />

	<style>
		* {
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}

		body {
			font-family: Arial, Helvetica, sans-serif;
		}

		.container {
			border-radius: 5px;
			width: 50%;
			margin: 2rem auto;
			padding: 2rem 0;;
			box-shadow: rgba(0, 0, 0, 0.24) 0px 3px 8px;
			background-color: antiquewhite;
		}

		.container h1 {
			text-align: center;
			color:rgb(55, 51, 51);
			margin: 1rem;
		}

		.form {
			margin: 30px;
			position: relative;
		}

		.form input{
			width: 100%;
			display: block;
			font-size: 1.5rem;
			border: none;
			border-radius: 5px;
			margin: 5px auto;
			padding: 5px;
		}

		.form button{
			width: 100%;
			height: 40px;
			border: none;
			border-radius: 10px;
			background-color: #000;
			color: white;
			font-size: 1.5rem;
			margin-top: 1rem;
			position: relative;
			z-index: 100;
			transition: all .2s;
		}	

		.form button:hover {
			transform: translateY(+5px);
			background-color: rgb(111, 107, 107);
		}

		.errors {
			border: 1px solid red;
			padding: 20px;
			margin: 20px;
			background-color: aliceblue;
			text-align: center;
		}
		.errors ul {
			list-style-type: none;
		}
	</style>
</head>

<body>
    <a href="/api/filmes" class="btn btn-back">Voltar</a>

	<div class="container">
		<h1>Formulário para Editar Filme</h1>
		<% if(errors.length > 0) { %>
			<section class="errors">
				<ul>
					<% for(let i = 0; i < errors.length; i++){ %>
						<li>
							<%= errors[i].msg %>
						</li>
					<% } %>
				</ul>
			</section>
		<% } %>
		<form class="form" action="/api/editar/<%= movie._id %>" method="post">
			<h2>Filme:</h2>
			<input type="text" name="name" placeholder="Digite o nome do filme" value="<%= movie.name %>">
			<h2>Diretor:</h2>
			<input type="text" name="director" placeholder="Digite o nome do diretor" value="<%= movie.director %>">
			<h2>Imagem:</h2>
			<input type="text" name="link" placeholder="Digite a url da foto da imagem" value="<%= movie.link %>">
			<button type="submit" value="enviar">Enviar</button>
		</form>
	</div>

</body></html>